apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chassishelloworld.fullname" . }}-test-connection"
  labels:
    {{- include "chassishelloworld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chassishelloworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
